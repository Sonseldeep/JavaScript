const male = "Sandeep";
const female = "XYZ";
var n1 = Math.random();
n1*=100  // its indicates that value range from 0 -100

n1=Math.floor(n1)+1;  // +1 indicates that possible to 100 
// floor vaneko   whole number only 
console.log("--------------------------");
console.log(`${female} ❤️  ${male} : ${n1}%`);
