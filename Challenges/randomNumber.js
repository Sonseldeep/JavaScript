var n = Math.random();
// its values is range from 0-0.999999 always
  // we set the values ranges from 0-6
n=Math.floor(n*6)+1;  // its gives the whole number only => removes decimal points

console.log(n);