// create a class user with 2 properties, name and email
// it also has method called viewData() that allows user to view theit data

class User {
  constructor(name, email) {
    this.name = name;
    this.email = email;
  }
}
let u = new User("Krishan");
