let foodItems = ["potato", "apple", "tommato"];
foodItems.push("Banana");
foodItems.push("Rasberry", "Candy", "Noodles", "Honey");
console.log(foodItems);

// push() add the items to the last of index

let deletedItems = foodItems.pop();
console.log(foodItems);
console.log(deletedItems);

// to string: ----> to convert array into string

let numbers = [1, 2, 3, 4];
console.log(numbers.toString());
